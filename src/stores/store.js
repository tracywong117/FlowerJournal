import { defineStore } from 'pinia';
import axios from 'axios';

export const useSearchStore = defineStore('search', {
  state: () => ({
    // input_query: '',
    

  }),
  actions: {
    // setInputQuery(new_query) {
    //   this.input_query = new_query;
    // },
    
  }
});